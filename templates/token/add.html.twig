{% extends 'base.html.twig' %}

{% block title %}Add Token{% endblock %}

{% block body %}

    <h1>Add a new Token</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}
